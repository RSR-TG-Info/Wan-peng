version: "3.9"
services:
  worker:
    build: .
    environment:
      REDIS_URI: $REDIS_URI
      REDIS_PASSWORD: $REDIS_PASSWORD
      SESSION: $SESSION
      API_ID: $API_ID
      API_HASH: $API_HASH
      MONGO_URI: $MONGO_URI
      DATABASE_URL: $DATABASE_URL
